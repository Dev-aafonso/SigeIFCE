
# ============================================================

# SIGE IFCE

# Ambiente PostgreSQL

# ============================================================

services:

postgres:
image: postgres:16
container_name: sige-postgres
restart: unless-stopped

`
environment:
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
  POSTGRES_DB: sige_ifce

ports:
  - "5432:5432"

volumes:
  - sige_postgres_data:/var/lib/postgresql/data
`

volumes:
sige_postgres_data:
